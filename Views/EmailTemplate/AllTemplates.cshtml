@model List<EMAILTEMPLATE>
@{
    Layout = "~/Views/Shared/_LayoutSecondary.cshtml";
}
@section Title {
    Email Şablonları
}
    @section Name1{
        
}
    @section Name2{
    Email Şablonları
}
    <table class="table ">
        <thead>
            <tr>
                <th scope="col">Başlık</th>
                <th scope="col">İçerik</th>
                <th scope="col">Login Gerekli</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr class="table">
                <td>@item.Name</td>
                <td>@item.Content</td>
                <td>@item.NeedToLogin</td>
                <td><a asp-controller="EmailTemplate" asp-action="EditTemplate" asp-route-id=@item.Id class="btn btn-secondary py-2 px-4 d-none d-lg-block">Düzenle</a></td>
            </tr>
        }
    </tbody>
</table>
